# ============================================
# Automated IOC Extractor v1.0
# Requirements File
# ============================================

# Core: No external dependencies required.
# All core functionality uses Python standard
# library modules listed below:
#
#   re         — Regex pattern matching
#   json       — JSON parsing and export
#   csv        — CSV file export
#   os         — File system operations
#   sys        — System and CLI operations
#   argparse   — Command-line interface
#   urllib     — VirusTotal API HTTP requests
#   hashlib    — Hash computation
#   uuid       — STIX ID generation
#   datetime   — Timestamps
#   getpass    — Secure API key input
#   base64     — URL encoding for VT API

# ── Optional packages (future enhancements) ──
# pdfplumber>=0.9.0       # PDF report parsing
# python-docx>=0.8.11     # Word document parsing
# beautifulsoup4>=4.12.0  # Advanced HTML parsing
# requests>=2.31.0        # Alternative HTTP client